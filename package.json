{
  "name": "foo-canvas",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "test": "jest",
    "start": "parcel src/index.html",
    "clean-start": "npm run clean && parcel src/index.html",
    "build": "npm run clean && parcel build src/index.html --public-url ./",
    "build-serve": "npm run build && http-server ./dist",
    "clean": "rimraf ./dist ./.cache"
  },
  "author": "Aaron Boyd",
  "license": "MIT",
  "dependencies": {
    "@types/pixi.js": "^5.0.0",
    "http-server": "^14.1.1",
    "parcel": "^2.12.0",
    "pixi.js": "^5.2.1",
    "prettier": "^3.3.3",
    "remeda": "^2.13.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2"
  }
}
